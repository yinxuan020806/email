# Email Helper 客户端独立依赖（PyInstaller 打包时只装这一份）。
#
# Stage 1：仅需 requests + pystray + Pillow（连通性测试 / 托盘 UI）。
# Stage 2 上 DrissionPage 浏览器自动化时再追加：
#   DrissionPage>=4.1,<5
#   pyautogui>=0.9.54,<1
#   opencv-python-headless>=4.8,<5
#   numpy>=1.26,<3
requests>=2.31,<3
pystray>=0.19,<1
Pillow>=10.0,<12
