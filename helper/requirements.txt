# Email Helper 客户端依赖（PyInstaller 打包时只装这一份）。
#
# Stage 1（连通性 + 托盘）：requests / pystray / Pillow
# Stage 2（DrissionPage 浏览器自动化 + Cloudflare 模板匹配）：DrissionPage /
#     pyautogui / opencv-python-headless / numpy
#
# 体积说明：opencv-python-headless 比 opencv-python 小 ~50MB（不带 GUI 部分，
# 模板匹配够用）。整体 PyInstaller onefile 输出约 60MB。

# ── Stage 1：连通性 + 系统托盘 ──
requests>=2.31,<3
pystray>=0.19,<1
Pillow>=10.0,<12

# ── Stage 2：邮箱浏览器自动化 ──
DrissionPage>=4.1,<5

# ── Stage 2 可选：Cloudflare Turnstile 自动通过（pyautogui + 模板匹配） ──
# 不装也能跑：遇到 Turnstile 时 helper 会在日志里要求用户手动点击。
# 装了体验更顺畅。
pyautogui>=0.9.54,<1
opencv-python-headless>=4.8,<5
numpy>=1.26,<3
