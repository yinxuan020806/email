# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
*.egg

# 用户数据 - 包含真实邮箱密码与主密钥
data/
*.db
*.db-wal
*.db-shm
.master.key

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
.windsurfrules

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Virtual Environment
venv/
.venv/
.env

# 日志
*.log
logs/

# Playwright / 测试残留
.playwright-mcp/
test-*.png

# 部署过程产物（截图、临时上传包）
*.zip
*.tar.gz
/app-*.png
/baota-*.png
/cf-*.png
/page*.png
/step*.png
/xterm-*.png

# Cursor / AI 工具元数据（保留 .cursor/rules，因为它是项目规范）
.cursor/projects/
mcps/
agent-transcripts/
terminals/

# 一次性远程执行脚本（含 SSH 密码读取逻辑，仅本机使用，绝不入库）
_deploy_remote.py
_deploy_remote.log

# 邮箱助手 Helper PyInstaller 产物：
# build.ps1 把打包好的 EmailHelper.exe 拷到 static/helper/ 给 Web 面板下载，
# .exe 体积 ~20MB 不适合入库；install/uninstall 脚本也是从 helper/ 同步过来，
# 源在 helper/ 已入库，整目录忽略即可。
static/helper/
helper/.build_venv/
helper/build/
helper/dist/
helper/*.spec.bak

# 给 git commit -F 用的临时 commit 消息文件（PowerShell 处理 heredoc 太麻烦）
_commit_msg.txt
_test_*.py
_smoke_data/

